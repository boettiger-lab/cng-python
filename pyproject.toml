[project]
name = "cng"
version = "1.0.0"

dependencies = ["minio",
                "ibis-framework[duckdb]",
                "duckdb",
                "streamlit",
                "pydeck",
		"boto3",
		"botocore",
		"subprocess",
		"tippecanoe",
		"re"]

[project.optional-dependencies]
tests = ["pytest", "pytest-cov"]

[tool.pytest.ini_options]
testpaths = ["tests"]
