[project]
name = "cng"
version = "1.0.0"

dependencies = ["minio",
                "ibis-framework",
                "duckdb",
                "streamlit",
                "pydeck"]

[project.optional-dependencies]
tests = ["pytest", "pytest-cov", "ibis-framework"]
